Dependencies:
SQLAlchemy 0.6.x
PyGTK (For the GTK interface)
PyQT (For the QT interface)
btrfs-tools

Install:
python install.py --interface={GTK|QT}
(You can specify only one --interface option)

If you want to "try before you buy", just run btrfsguitools.py --interface={GTK|QT|cmd}
(You can specify only one --interface option)
