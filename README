Dependancies:
SQLAlchemy 0.5.x
PyGTK (For the GTK interface)
PyQT (For the QT interface)
btrfs-tools

Install:
python install.py --interface={GTK|QT|cmd}
(You can specify as many --interface options as you want)